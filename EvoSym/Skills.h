/**
* @file Skills.h
* @brief: This class file contains all Skills which an animal can have. 
* @date 10.07.2017
* @author Jakob Wandel
* @version 0.0 experimental
**/
#ifndef _SKILLS_
#define _SKILLS_

#include "structs.h"

/*
class Climb{
	bool possible(double strength, double weight, AnimalSkin& skin, AnimalBuild& build);
};

class Dig{
	bool possible(double strength, double weight, AnimalSkin& skin, AnimalBuild& build);
};

class Swim{
	bool possible(double strength, double weight, AnimalSkin& skin, AnimalBuild& build);
};

class Dive{
	bool possible(double strength, double weight, AnimalSkin& skin, AnimalBuild& build);
};

class Fly{
	bool possible(double strength, double weight, AnimalSkin& skin, AnimalBuild& build);
};

class Move {
	bool possible(double strength, double weight, AnimalSkin& skin, AnimalBuild& build);
};

*/
#endif // _SKILLS_

